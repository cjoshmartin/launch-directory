<h1>Twitter</h1>

<table>
    <tr>
        <th>name</th>
        <th> Bio</th>
        <th> Twitter handle</th>
    </tr>

    <% @entities.each do |entity| %>
    <tr>
      <td><%= entities.name %></td>
      <td><%= entities.description %></td>
      <td><%= entities.twitter %></td>
    </tr>
    <% end %>
</table>

    <br>
    <%= link_to "entity", new_entity_path %>    
